{
  "name": "dashco",
  "version": "0.2.0",
  "private": true,
  "description": "Automatic-company starter on gstack. AI is not load-bearing.",
  "type": "module",
  "scripts": {
    "demo": "bun run src/demo.ts",
    "day-one": "bun run bin/day-one.ts",
    "server": "bun run bin/server.ts",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@electric-sql/pglite": "^0.4.6",
    "drizzle-orm": "^0.45.2",
    "hono": "^4.12.23",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^20.14.10",
    "typescript": "^5.5.3",
    "vitest": "^2.0.2"
  }
}
